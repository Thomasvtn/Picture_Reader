#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "reseaun.h"
#include "Network.h"

void add_layer(Network net, int size)
{
    if(net -> layersize > 0 )
    init(net -> Layer,size)
}
